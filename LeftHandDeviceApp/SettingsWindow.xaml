<Window x:Class="LeftHandDeviceApp.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="設定" Height="440" Width="500"
        Icon="app_icon.ico"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- アプリ情報 -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,15" Padding="15">
            <StackPanel>
                <TextBlock Text="アプリケーション情報" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="バージョン:" VerticalAlignment="Center" Margin="0,0,0,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="VersionText" Text="v1.0.0" VerticalAlignment="Center" Margin="0,0,0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="アプリ名:" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="LeftHandDevice" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- テーマ設定 -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,15" Padding="15">
            <StackPanel>
                <TextBlock Text="テーマ設定" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="ThemeSystem" Content="システムに合わせる" GroupName="Theme" 
                                 Margin="0,0,20,0" Style="{StaticResource MaterialDesignRadioButton}"
                                 Checked="Theme_Changed"/>
                    <RadioButton x:Name="ThemeLight" Content="ライト" GroupName="Theme" 
                                 Margin="0,0,20,0" Style="{StaticResource MaterialDesignRadioButton}"
                                 Checked="Theme_Changed"/>
                    <RadioButton x:Name="ThemeDark" Content="ダーク" GroupName="Theme" 
                                 Style="{StaticResource MaterialDesignRadioButton}"
                                 Checked="Theme_Changed"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <!-- アップデート -->
        <materialDesign:Card Grid.Row="2" Margin="0,0,0,15" Padding="15">
            <StackPanel>
                <TextBlock Text="アップデート" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="UpdateStatusText" Grid.Column="0" 
                               Text="アップデートを確認してください" 
                               VerticalAlignment="Center" TextWrapping="Wrap"/>
                    <Button x:Name="CheckUpdateButton" Grid.Column="1" 
                            Content="アップデート確認" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Click="CheckUpdate_Click" Margin="10,0,0,0"/>
                </Grid>
                <ProgressBar x:Name="UpdateProgress" Margin="0,10,0,0" 
                             Visibility="Collapsed" IsIndeterminate="True"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- 閉じるボタン -->
        <Button Grid.Row="4" Content="閉じる" 
                Style="{StaticResource MaterialDesignOutlinedButton}"
                HorizontalAlignment="Right" Width="100"
                Click="Close_Click"/>
    </Grid>
</Window>
